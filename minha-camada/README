# Minha Camada

Esta é uma camada Yocto personalizada criada para o projeto Poky.

## Descrição

A camada "minha-camada" fornece receitas customizadas e configurações específicas do projeto.

## Dependências

Esta camada depende de:
- OpenEmbedded-Core (meta)
- Poky (meta-poky)

## Adicionando à configuração

Para usar esta camada, adicione o seguinte ao arquivo `conf/bblayers.conf`:

```
BBLAYERS += "${BSPDIR}/../minha-camada"
```

## Estrutura de diretórios

- `conf/layer.conf` - Configuração da camada
- `recipes-example/` - Exemplo de diretório com receitas

## Suporte

Para mais informações sobre camadas Yocto, consulte a documentação oficial do Yocto Project.
